:5274 {
  # OPTION: habilitar compresi??n (version anterior):
  # encode zstd gzip

  # OPTION: matcher expl??cito para WS (version anterior):
  # @ws path /ws
  # reverse_proxy @ws 127.0.0.1:3000

  # Proxy todo (incluye /health y rutas WS)
  reverse_proxy 127.0.0.1:3000
}
